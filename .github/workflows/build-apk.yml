name: Build and Publish Amy FX APK

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow temporarily paused while preparing Amy FX 1.4.7"
